Switch from .terminal to ansiTerm

Finish conversion to c++_pykeProject, excise project, cpp_project

Finish conversion for c++_tool

Rejigger tests (again), add E2E tests

Maybe hoist ObjectData to its own package

Clean up docstrings, etc

propagate commands to dependent projects

add -wrath to clean to fully wipe directories

add verbosity to all commands

add rebuild alias

copy res/* to bin/config/res/*

-fsanitize=address for debug builds
-fsanitize-address-use-after-scaope

Rename generated run script to be named per-project, so no stompy-stompy.

pkg-config command line options support in .json. (--static, etc)

Allow $var expansion in string fields (for paths, etc).

Allow %var expansion in string fields.

Handle paths with spaces in'em :(
